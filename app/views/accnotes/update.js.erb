$('body').removeClass('modal-open');
$('.modal-backdrop').remove();
$('.accnote').html("<%= j (render 'accnotes/account_note') %>");
$('.acctimelines').html("<%= j (render 'acctimelines/timelines_account') %>");
$('#notice-accnote').html("<%= j (render 'layouts/message') %>").delay(1600).fadeOut();
