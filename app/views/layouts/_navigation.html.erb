<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#crm-menu">
                <span class="sr-only">Toggle Navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>

        <%= link_to root_path, class: "navbar-brand" do %>
        Local Basic CRM
        <% end %>

        <% if user_signed_in? %>
        <div class="navbar-nav" style="margin-top: -15px; width: 30%;">
            <%#= simple_form_for :search, method: :get do |f| %>
            <%#= f.input :query, placeholder: "Search Local Basic CRM" %>
            <%# end %>
        </div>

        <div class="collapse navbar-collapse" id="crm-menu">
            <%= render 'layouts/menu' %>
        </div>
        <% end %>
    </div>
</nav>
