$('#doc').html("<%= j (render 'documents/doc') %>");
$('#case-details').html("<%= j (render 'cases/case_details') %>");
$('#case-header').html("<%= j (render 'cases/case_header')%>");
$('#stage').html("<%= j (render 'stages/stage')%>");
$('#case-title').html("<%= j (render 'cases/case_title')%>");
$('.timelines').html("<%= j (render 'timelines/timelines_index') %>");

$('#notice-doc').html("<%= j (render 'layouts/message') %>").delay(1600).fadeOut();

$('#opportunityModalForm').modal('hide');
$('#crm-menu').html("<%= j (render 'layouts/menu') %>");

$('#docs_').val('');

$('#assignContactForm').modal('hide');
$('#contact').html("<%= j (render 'contacts/contact') %>")
$('#notice-contact').html("<%= j (render 'layouts/message') %>").delay(1600).fadeOut();

var collection = []
collection = $('#assigned_').val();
for(i = 0; i < collection.length; i++){
  $('.selectpicker').find('[value=' + collection[i] +']').remove();
  $('.selectpicker').selectpicker('refresh');
}
