<div class="tab-pane fade in active" id="opportunityActivities">
  <h4>Opportunity Activities</h4>
  <div class="panel panel-default panel-body">
      <ul class="nav nav-tabs">
          <li class="active"><%= link_to 'New Task', '#task',
                  data: {toggle: "tab", target: "#task, #task_list"} %></li>
          <li><%= link_to 'Log Call', '#call',
                  data: {toggle: "tab", target: "#call, #call_list"}%></li>
          <li><%= link_to 'New Event', '#event',
                  data: {toggle: "tab", target: "#event, #event_list"}%></li>
      </ul>

      <br>
      <div class="tab-content">
          <%= render 'opportunity_activities_forms' %>
        </div>
  </div>

  <br>

  <div class="tab-content">
  <%= render 'opportunity_activities_tasks' %>
  <%= render 'opportunity_activities_calls' %>
  <%= render 'opportunity_activities_events'%>
</div>
</div>
