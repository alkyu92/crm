$('#user_accounts').html("<%= j (render 'users/user_accounts') %>");
$('#user_opportunities').html("<%= j (render 'users/user_opportunities') %>");

$('#user_tasks').html("<%= j (render 'users/user_tasks') %>");
$('#user_calls').html("<%= j (render 'users/user_calls') %>");
$('#user_events').html("<%= j (render 'users/user_events') %>");
$('#user_marketings').html("<%= j (render 'users/user_marketings') %>");
$('#user_cases').html("<%= j (render 'users/user_cases') %>");
