$('.modal').modal('hide');
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();

$('.timelines').html("<%= j (render 'timelines/timelines_index') %>");
$('.task').html("<%= j (render 'tasks/task') %>");
$('#notice-task').html("<%= j (render 'layouts/message') %>").delay(1600).fadeOut();
$('#crm-menu').html("<%= j (render 'layouts/menu') %>");
